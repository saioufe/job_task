import 'dart:async';
import 'package:job_task/models/work_card_model.dart';

class WorkRepository {
  const WorkRepository();

  @override
  Future<CardWorkModel> getWorkUserId(int id) async {
    try {
      //write code here
    } catch (e) {
      print(e.message);
    }
  }
}
